/**
 * Kept as a stable import path. The one voucher card now lives in VoucherCard.jsx
 * and is theme-aware (real light + dark surfaces) using the design-system
 * primitives in components/ui/.
 */
export { default, default as HomepageVoucherCard } from './VoucherCard.jsx';
